<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;
use App\Models\Reservation;

class ReservationsTableSeeder extends Seeder
{
    /**
     * Run the database seeds.
     *
     * @return void
     */
    public function run()
    {
        // Reservation::factory()->count(1)->forShop(['shop_id' => '1',])->forUser(['user_id' => '1'])->create();
        // Reservation::factory()->count(1)->forShop(['shop_id' => '2',])->forUser(['user_id' => '1'])->create();
        // Reservation::factory()->count(1)->forShop(['shop_id' => '3',])->forUser(['user_id' => '1'])->create();
    }
}
